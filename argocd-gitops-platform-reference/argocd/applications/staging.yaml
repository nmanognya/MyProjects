apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: podinfo-staging
  namespace: argocd
spec:
  project: podinfo-platform
  source:
    repoURL: https://github.com/nmanognya/MyProjects.git
    targetRevision: main
    path: argocd-gitops-platform-reference/workloads/podinfo/overlays/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: podinfo-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
  revisionHistoryLimit: 10
